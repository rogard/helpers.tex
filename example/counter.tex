% arara: clean: { extensions: [ aux, toc, lof, lot, out, bbl, bcf, erw.counter.aux, listing ] }
% arara: pdflatex
% arara: pdflatex
% arara: clean: { extensions: [ aux, toc, lof, lot, out, bbl, bcf, erw.counter.aux, listing ] }

\documentclass{article}

\usepackage{lipsum,tcolorbox,graphicx}
\tcbuselibrary{listings,raster,skins}
\newtcblisting{listing-tex}{
  listing and text,
  listing options={language=Tex, style=tcblatex}
}

\usepackage{erw-counter}

\CounterSetup{ new = foo }

\begin{document}

\begin{listing-tex}
  % \ExplSyntaxOn
  % \file_input:n{erw-counter.sty}
  % \ExplSyntaxOff
  % \CounterSetup{ new = foo }

  % Expected: 0
  Initial value: \CounterUse{foo}

  % Expected: 5
  Final value: \CounterRetrieve{foo}{final}

  \CounterIncrement{foo}
  \CounterIncrement{foo}
  \CounterIncrement{foo}

  % Expected: 3
  Value after 3 increments: \CounterUse{foo}

  \CounterIncrement{foo}
  \CounterIncrement{foo}
  \CounterRemember{foo}{final}
\end{listing-tex}

\end{document}
